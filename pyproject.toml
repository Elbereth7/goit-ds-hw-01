[tool.poetry]
name = "goit-ds-hw-01"
version = "0.1.0"
description = "Module 1. Python Development. Docker"
authors = ["Nataliya Yakovenko"]
readme = "README.md"

[tool.poetry.dependencies]
python = "3.12.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
